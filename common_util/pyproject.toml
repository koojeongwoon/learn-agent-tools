[project]
name = "common_util"
version = "0.1.0"
description = "Project-wide shared utilities"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["common_util"]

[tool.setuptools.package-dir]
common_util = "."
